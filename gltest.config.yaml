networks:
  default: localnet
  localnet:
    url: "http://127.0.0.1:4000/api"
    leader_only: false
    default_wait_interval: 3000
    default_wait_retries: 100
  studionet:
    leader_only: false
    default_wait_interval: 3000
    default_wait_retries: 150

paths:
  contracts: "contracts"
  artifacts: "artifacts"

environment: .env
