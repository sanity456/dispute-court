# Core/helper addresses and Studionet RPC are pinned in lib/*deployment.json.
# Browser and server share those manifests; address/RPC environment overrides are not used.
# Optional Vercel target only. Configure secrets through the new Vercel project.
# DATABASE_URL=
# Wallet sign-in uses server-generated one-time challenges; no email auth provider.
# NEXT_PUBLIC_SITE_ORIGIN=
