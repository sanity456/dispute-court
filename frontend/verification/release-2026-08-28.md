# Release verification — 2026-08-28

## Verified source and build

- `pnpm format:check`: passed.
- `pnpm test`: 50 passed, zero failed, zero skipped.
- `pnpm typecheck`: passed.
- `pnpm lint`: passed with zero warnings.
- `CODEX_LOCAL_PREVIEW=0 pnpm build`: passed through client, RSC and SSR production output.
- Selected core v2 and evidence-helper direct contract tests: 65 passed.
- Evidence-helper GenVM lint: passed.

The shared tests cover account isolation, atomic intent reservation, immutable transaction hashes, RPC outages, exact wei, receipt/request matching, reverted/unknown execution, independent child-payout verification, owner-wallet proof, support privacy, directory coverage and stale details, reminders, wallet/network changes, and bounded operator policy.

The build's route-classification and plugin-timing notices are toolchain diagnostics, not test passes or errors. Production uses the Worker/D1 adapter; local SQLite is not packaged as user data.

## Live Studionet proof

See [source-lifecycle-2026-08-28.json](./source-lifecycle-2026-08-28.json).

The explicitly marked automated fixture `verified-source-mtcs55f3` completed 11 finalized successful parent transactions, including consensus-backed public-source capture, full source/digest verification, the product lifecycle, and independently verified native payout delivery. These are synthetic test participants, not adoption metrics. The operator dry run sent no transaction.

Local API checks earlier in this finishing pass verified the provided local sign-in identity, anonymous denial, live contract configuration, durable preferences/support, real receipt import, exact credited payout and the owner-proof requirement. No synthetic local support ticket or database was copied into production.

## Dependency audit

The final full version-based scan reports only two high advisories for upstream `image-size@2.0.2` (GHSA-w3rx-r6r6-pgpr and GHSA-5p2g-fcmc-qvqq). Its known non-progressing image-parser loops are mitigated by the checked-in pnpm patch. The regression suite verified malformed ICNS, HEIF and JXL cases against both distributed module formats, with child-process timeouts, while preserving valid PNG/ICNS dimensions.

No advisory was suppressed and no unpublished package version was invented. This is not an independent security certification. See [the operating guide](../docs/OPERATIONS.md) for patch maintenance.

## Deliberately not claimed

- A final complete browser/mobile/two-wallet acceptance run. Browser automation timed out during this pass; the earlier UI spot checks and signed SDK journeys are not a substitute for human wallet acceptance.
- Closed-app push/email delivery, an always-on signer, a configured external backup service or an independent security/AI-policy review.
- Public beta access, real-money readiness or GitHub publication.

Hosted deployment and endpoint results are recorded separately in the workspace release handoff after publication. Both products retain independent Sites, databases and contract configuration.
